# abs_worker Environment Configuration
# Copy this file to .env and customize for your environment

REQUIRED_CONFIRMATIONS=6
MAX_RETRIES=3
RETRY_DELAY=5
WORKER_TIMEOUT=300
MAX_CONCURRENT_TASKS=10
LOG_LEVEL=INFO
ENVIRONMENT=development


DB_HOST=10.237.48.188
DB_PORT=5432
DB_USER=fullon
DB_PASSWORD=
DB_NAME=abs_notary
DB_TEST_NAME=abs_notary_test


# Connection Pool Configuration (matching fullon_orm)
DB_POOL_SIZE=20                # Number of persistent connections in the pool
DB_MAX_OVERFLOW=10             # Maximum overflow connections above pool_size
DB_POOL_PRE_PING=true          # Check connection health before using
DB_POOL_RECYCLE=3600           # Recycle connections after this many seconds (1 hour)
DB_POOL_TIMEOUT=30             # Timeout in seconds for getting connection from pool

# Logging Configuration
LOG_LEVEL=DEBUG
SQL_ECHO=false

